This is an advanced version of snake.

You can move around the grid using the arrow keys.

To run the game, simply double click the AdvancedSnake Jar file.
You can also run the game, by accesing it with the command prompt

java -jar ./AdvancedSnake.jar

